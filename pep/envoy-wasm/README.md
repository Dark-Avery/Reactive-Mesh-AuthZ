# Envoy WASM

В текущем репозитории основной реактивный PEP реализован как нативный C++-фильтр Envoy в каталоге [envoy-custom/reactive_pep](../../envoy-custom/reactive_pep).

Каталог `pep/envoy-wasm` сохранён как часть структуры проекта и фиксирует альтернативный путь реализации Envoy-расширения.
